{
  "name": "abif-funding-radar-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node -e \"Promise.all(['./api/health.js','./api/trigger-email.js','./api/trigger-sync.js','./api/send-feedback.js'].map((path) => import(path))).then(() => console.log('api-check-ok'))\""
  },
  "dependencies": {
    "nodemailer": "^8.0.1"
  }
}
